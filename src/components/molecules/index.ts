/**
 * Molecules - Composed UI components
 *
 * These are combinations of atoms that function together as a unit.
 * Examples: Logo (Icon + Text), NavLink (Link + Styles), SectionHeader (Badge + Heading + Text)
 */

// Logo
export { Logo } from "./Logo";
export type { LogoProps } from "./Logo";

// Navigation Link
export { NavLink } from "./NavLink";
export type { NavLinkProps, NavLinkVariant } from "./NavLink";

// Section Header
export { SectionHeader } from "./SectionHeader";
export type { SectionHeaderProps, SectionHeaderAlignment } from "./SectionHeader";

// Email Input
export { EmailInput } from "./EmailInput";
export type { EmailInputProps } from "./EmailInput";
